# frozen_string_literal: true

require 'etc'
require 'debug'

class Ls
  def initialize(receive_options)
    
  end
end

file_and_directory_name = FileAndDirectoryName.new(receive_a_option: receive_options['a'])


# binding.break

# @display = receive_options['r'] == true ? @frames.reverse : @frames
# output_method = receive_options['l'] == true ? long_filename_output : default_output
